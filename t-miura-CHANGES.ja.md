# t-miura/JK1KHWによるPlaiの変更点

本ドキュメントでは、[私のフォーク](https://github.com/t-miura/plai/)の `development` ブランチに統合されたPlaiの作業およびアーキテクチャの変更点について詳しく説明します。このブランチは、ESP32-S3ベースのCardputer ADV+Cap LoRa-1262上で軽量高速に動作するd4rkmen氏開発の[Plai](https://github.com/d4rkmen/plai)を、より信頼性の高いスタンドアロンMeshtasticノードにすることに重点を置いています。緻密なメモリ最適化、並行処理（コンカレンシー）の管理、ハードウェアの電力状態の制御を通じて、リソースが限られた環境（PSRAMなし、512KB SRAM）の制約を見事に解決しました。同時に、[d4rkmenによるアップストリームの `main` コードベース](https://github.com/d4rkmen/plai/tree/main)の美しい設計フローを維持しており、ユーザーが新しい動作を可能な限り選択できるよう、変更の大部分は設定メニューからのオプトイン/オプトアウト（有効/無効の切り替え）オプションとして提供されています。

## 1. 高度な省電力とLight Sleep（ライトスリープ）の統合

*これはこれまでで最も困難な課題でした。* バッテリーの駆動時間を向上させるために、真のESP32電力管理とCPUのLight Sleepを実装する必要がありました。

* **動的な周波数スケーリングとスリープ**: `esp_pm` を統合し、真のライトスリープを許容しつつ、CPUクロックを最大240MHzから最小のXTAL 40MHzの間で動的にスケーリングするようにしました。
* **インテリジェントなPMロック**: システムがインテリジェントに `esp_pm_lock_handle_t` (`_cpu_lock`) の取得と解放を行うようになりました。ディスプレイやGPSモジュールがアクティブな間はCPUロックが保持され、ディスプレイがオフになりGPSがスリープ状態になると、システムはロックを解放します。
* **割り込みの安全性**: ライトスリープ中のハードウェア割り込みの処理が大きなハードルでした。ウォッチドッグによるロックアップを回避するため、スリープ直前にGPIOピン11（キーボード）とピン4（無線）をレベルトリガーのウェイクアップとして設定し、ウェイクアップ直後にエッジトリガーの割り込みへ適切に戻すようにしました。

## 2. ハードウェアとGPSの管理

* **スリープ可能なGPS**: CASIC ATGM336H GPSモジュールが、GPS非使用時に `$PCAS12,65535` コマンドを介してソフトウェアのスリープモードに適切に従うようになりました。GPSモジュールを確実にスリープ状態に保つため、10分周期のウェイクアップタスクが実行されます（`gps.cpp`）。GPSがスリープ中は、NMEAメッセージカウンターも確実に停止します。この動作は、設定からオフにしてd4rkmenのコードの挙動に合わせることも可能です。また、継続的にGPSロックを取得できる良好な場所であれば、非常に正確なGPSベースの置き時計として機能させることもできます。
* **RTCのブートストラップと定期同期**: ATGM336Hの32kHz XTALとスーパーキャパシタでバックアップされたメモリを活用し、ZDAセンテンス (`$PCAS03`) を有効にしてESP32-S3の内部RTCを迅速に同期するようにしました。ESP32-S3の内部8MD256オシレーターは本質的にドリフト（ズレ）が発生しやすく、かといってユーザーにStampS3Aへの32kHz XTALのハードウェア改造を強いるのは「買ってすぐ書き込む（buy-and-flash）」という私たちの哲学に反するため、比較的信頼性の高いGPSのRTCから定期的にソフトウェア同期を行い、内部時計のドリフトを継続的に補正する機能を実装しました。ATGM336HのRTCは、実際の時間を取得するためのロックなしでも数日以上にわたって正確に時間を刻みます（1秒以上ドリフトするのに数週間かかるかもしれません…まさに素晴らしい時計です！）。ただし、内部のスーパーキャパシタ/バッテリーを枯渇させないように注意が必要です。可能であれば、完全に充電された状態を保つために、ノード全体を24時間365日稼働させておくことをお勧めします。
* **メッシュ時刻同期（「最後の手段」としてのフォールバック）**: ローカルの時計が未設定の場合（スーパーキャパシタの放電などによる）、システムは自動的にプライマリチャネルの受信 `Position` パケットをスニッフィングし、UIのGPSロックインジケーターを誤検知させることなくローカルRTCを同期します（`mesh_service.cpp`）。この機能も設定で無効にすることができます。これは、時計がズレている既知のノードが、Positionパケットで不正確な時間データを送信している場合に有用です。将来の変更では、公式のMeshtasticファームウェアと同様に、ローカルの時計が遅れている場合にのみメッシュ経由での同期を許可することで、Positionパケットに誤って同期してしまうリスクを軽減する予定です。これでは実際よりも進んだ時間を送信するノードには効果がないため、追加の安全対策としてGPS時刻同期のしきい値とは別のしきい値を設定する必要があるかもしれません。

## 3. システムの安定性と並行処理（コンカレンシー）の修正

* **SDカードによる偶発的な再起動**: SDカードへのアクセス時に、SPIの競合が原因で偶発的に再起動が発生することがありました。無線の初期化中にCSピン（GPIO 12）を手動でプルアップし、SPI2バスの `max_transfer_sz` を4092バイトに増やしました。また、SDカードのSPIクロックを20MHz（このモードの標準周波数）から10MHzに下げました。以前経験した問題はSanDisk特有のSPIモードのバグに関連していた可能性がありますが、この新しい周波数はSX1262で使用されているものと一致するため、周波数を同期させることで問題が少なくなるはずです。
* **無線のバッファスタックオーバーフローのリスク**: LoRaヘルパー関数内の大きなローカル `tx[256]` 配列を排除し、クラスレベルの共有メンバーに移動することで、深刻なスタックオーバーフローのリスクを取り除きました。また、SPIアクセス（`sx1262.cpp`）の周囲にミューテックスロック（`_spi_mutex`）を強制する、スレッドセーフな `spiTransferLocked()` 関数を追加しました。
* **データの同期**: FreeRTOSの `SemaphoreHandle_t` を導入することで、NMEAパース中の競合状態（レースコンディション）とTorn Read（不完全な読み取り）を防ぎました。

## 4. メモリの最適化とクラッシュの修正

メモリが限られているため（mainブランチでは空きヒープが一桁KBとなることがしばしばありました）、これらの最適化は非常に重要でした。現在、アクティブな使用中の空きヒープは76KB〜104KBの間で安定しています。

* 使用されていないRSSIポイントのマッピング/トラッキング（`_rssi_history`）を無効にしました。これは以前、約26KBのヒープを消費していました（`mesh_data.cpp`）。d4rkmenがこのデータを表示する機能を追加した際には、ヒープを節約するための調整を加えつつ、元に戻す予定です。
* オンデマンドのノードインデックス作成に切り替えました。起動時に永続的に確保されていた36KBの `MAX_NODES` ベクター割り当てを、動的なサイジングに置き換えました（`node_db.cpp`）。

* **（検証によりリバート済）FATFSの構成**: SDカードの物理セクターに合わせるため、`sdkconfig` の `CONFIG_FATFS_SECTOR_4096` を `CONFIG_FATFS_SECTOR_512` に変更し、開いているファイルハンドルごとに3.5KBを節約して「SDカードが見つかりません」エラーを緩和しました。これが読み書きの操作に大きな影響を与えることは確認されていますが、現在は4096バイトのセクターアクセスを使用するように元に戻しています。他の取り組みによって十分なヒープが解放されたため、これを再導入しても安全と思われます。これにより、よりスムーズな使用が可能になり、SDカードへのアクセスの遅さによるロックアップのリスクが軽減される可能性があります。
* **アンバッファド・ストリーム**: C言語ストリームに対するnewlibのデフォルトである4KBのヒープ割り当てバッファをバイパスするため、ファイルローカルな `fopen` 操作をバッファリングなし（`_IONBF`）で実行するように設定しました。
* **AppNodesの `LoadProhibited` パニック**: `onCreate()` 実行時にガベージ状態の変数（`_data` メンバー）を徹底的に初期化し、脆弱なポインタ減算を安全な境界チェック付きのループに置き換えることで、ノードUIを終了する際のクラッシュを修正しました（`app_nodes.cpp`）。
* **[実験的] newlibc から picolibc へ**: 現在利用しているIDF-ESP 5.5.4では実験的な機能ではありますが、[公式ブログ](https://developer.espressif.com/blog/2026/04/esp-idf-6-default-libc-picolibc/)によると、libc を newlibc から picolibc に切り替えることで多くのメリットが得られます。そのため、現在のコードベースでは picolibc を使用し、マップの読み込み/レンダリングロジックにいくつかの修正を加えて、この 2 つの動作のギャップを埋めており、これまでのところすべての関数で正常に動作しています。

## 5. UIとユーザーエクスペリエンス（UX）の向上

* **絵文字フォントのサポート（超低解像度）**: 12x12のモノクロ絵文字を確実に2値化するため、平均適応型のしきい値処理スクリプト（`emoji_converter.py`）を実装しました。生成されたC++配列（`builtin_emojis.cpp`）は `emoji_draw_callback` を介してクリーンに統合されており、SDカードの遅延なしに高速なUIレンダリングを実現します。d4rkmenによるオリジナルの「SDカードからの絵文字PNG」機能も引き続きアクティブであるため、現在のアップストリームリリースと同様に、不足している絵文字やその他の文字はSDカードから読み込まれます。わずかなロード遅延と引き換えに、視覚的に美しいSDカードからの絵文字を厳密に使用するための「内部フォントからの絵文字」を無効にするオプションを追加する予定です（アップストリームと同様に、メモリ内に最大10個の絵文字がキャッシュされるのでご安心ください！）。
* **設定の永続化**: NVSを使用して、再起動後もUIの状態が適切に保持されるようになりました。ノードリストのソート順とマップのズーム設定は `system` 名前空間に保存されます（`settings.cpp`）。
* **マップズームの安定性**: 高速なキーリピートの更新ループから同期的なNVSへの書き込みを削除し、マップビューを終了する際にのみズーム設定をNVSにコミットするようにしました。これにより、マップを素早くナビゲーションする際のUIのフリーズが解消されます。
* **キークリック音の切り替え**: 標準の **Settings → System** メニュー内に、キーボードのクリック音を切り替える設定を追加しました。標準のアラート音は維持しつつ、ハードウェア抽象化レイヤー内でクリック音の再生をインターセプト（制御）します。
* **再起動 / シャットダウンメニュー**: デバイスを安全に再起動または電源オフにするため、これら2つのメニューを **Settings → Power** に配置しました。デバイス全体の電源を完全に切ることはできないため、シャットダウンを実行するとWindows 95風のスプラッシュスクリーンが表示され、デバイスの電源を落としても安全であることが通知されます（バッテリーススイッチをオフにスライドするか、USB-Cのみで動作している場合はケーブルを抜いてください）。
  * ![Safe to power off](pics/shutdown_screen.jpg)
